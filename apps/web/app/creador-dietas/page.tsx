import DietBuilder from "../../components/DietBuilder";

export default function CreadorDietasPage() {
    return (
        <main className="min-h-[calc(100vh-80px)] bg-neutral-950">
            <DietBuilder />
        </main>
    );
}
